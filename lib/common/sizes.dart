import 'package:flutter/widgets.dart';

const scaffoldPadding = EdgeInsets.all(15);

//Vertical
const vertical10= EdgeInsets.symmetric(vertical: 10.0);
const vertical5= EdgeInsets.symmetric(vertical: 5.0);

//Horizontal
const horizontal10= EdgeInsets.symmetric(horizontal: 10.0);
const horizontal5= EdgeInsets.symmetric(horizontal: 5.0);

//Right
const right20= EdgeInsets.only(right: 20.0);
const right10= EdgeInsets.only(right: 10.0);
const right5= EdgeInsets.only(right: 5.0);

//All
const all10= EdgeInsets.all(10);
const all5= EdgeInsets.all(5);

//TOP
const top10= EdgeInsets.only(top: 10);